name: Remove Hit Debug Circles

on:
  push:
    branches:
      - perf/high-star-load-20260905
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/verify-remove-hit-debug-circles-20260905.mjs
